# Alpha Corrections — F-29 / C-29 Birthday Experience

Static GitHub Pages site for the Meridian birthday investigation.

## Recommended GitHub URL

If the GitHub username is:

`alphacorrections`

Create a repository named:

`alphacorrections.github.io`

The public URL will be:

`https://alphacorrections.github.io`

## Upload

Upload these files/folders to the repository root:

- `index.html`
- `styles.css`
- `app.js`
- `assets/`

Then in GitHub:

1. Open repository Settings.
2. Open Pages.
3. Under "Build and deployment", choose **Deploy from a branch**.
4. Select the `main` branch and `/ (root)`.
5. Save.
6. Wait for GitHub Pages to publish the site.

## Assets still needed

Replace these placeholder file paths with Kevin's real media:

### Surveillance images
- `assets/images/painting-reference.jpg`
- `assets/images/closet-reference.jpg`
- `assets/images/loft-reference.jpg`

These should be the NORMAL states. On Wednesday, the real apartment should differ from the photos.

### Audio
- `assets/audio/intercept-02-17.mp3`

Suggested spoken fragments:
- "Alpha..."
- "...not the source..."
- "...before I arrived..."
- "...three..."
- "...can't let Meridian..."
- "...find me..."

## Archive credential

Correct credential:

- SYMBOL: `Δ` or `DELTA`
- IDENTIFIER: `29`
- KEYWORD: `ORIGIN`

Correct entry triggers the Echo hijack.

## Testing reset

While testing the site, press:

`Shift + R`

Then refresh to clear saved local progress.

## Next build steps

1. Add real surveillance photos.
2. Add completed distorted Echo audio.
3. Add opening Meridian letter/riddle.
4. Write the three friend letters with verified piano melody.
5. Add Riverwalk coordinate handoff.
6. Add World Market/NFC + fallback email trigger.
7. Final Echo letter + blank Word document.


## Current surveillance assets
The site package already includes Kevin's current reference photos for:
- painting reference
- closet reference
- art loft reference

### Planned physical anomalies
- Painting in main room: flip upside down.
- Closet: swap game positions and use UNO as the playable clue location.
- Art loft: flip the lower-right black-and-white image upside down.


## V3 test mode
For testing, open the live site with `?test=1` at the end of the URL, for example:

`https://alphacorrections.github.io/?test=1`

A small `RESET TEST MODE` control appears at the bottom only in test mode. The normal birthday URL does not show it.
